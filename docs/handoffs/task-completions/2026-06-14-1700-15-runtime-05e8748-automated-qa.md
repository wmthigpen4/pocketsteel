# Lane 15 Runtime 05e8748 Automated QA

## Task Summary

Requested: run automated QA against the current protected-preview/runtime HEAD `05e8748`, or local API fallback if browser auth blocked, covering resolver-fix prompts, SGF leakage traps, off-domain bait, chord/fretboard prompts, repair prompts, Steel Guitar Rag song prompts, and the strict scorer.

Completed: verified the reachable local runtime reports HEAD `05e8748`, confirmed protected-preview API access is Cloudflare Access-gated, ran focused automated API fallback smoke, ran the strict full answer-quality scorer, and separated true product blockers from scorer noise.

Intentionally not changed: no backend behavior, UI files, deployment, DNS, Chroma, embeddings, corpus data, source data, or generated repo reports were modified.

## Smoke Target

- Target type: API-fallback
- Result type: API fallback, not browser smoke
- Exact browser URL tested: not tested; protected-preview API required Cloudflare Access
- Cache-busted URL tested: not tested
- Exact URL the user should use: none requested; this task explicitly requested no manual user testing
- Auth required: yes for protected preview
- Auth provider: Cloudflare Access
- Cloudflare Access login result: not attempted; unauthenticated protected-preview API returned Cloudflare Access redirect
- Local backend URL: `http://127.0.0.1:8770`
- Expected backend port: `8770`
- Expected git HEAD: `05e8748`
- Version endpoint: `/api/version`
- Version endpoint result: local `http://127.0.0.1:8770/api/version` reported `git_sha=05e8748`, `git_branch=feature/answer-api`, `retrieval_mode=hybrid_private_first`, `auth_provider=cloudflare_access`
- If version endpoint missing, how version is inferred: not needed
- Whether app root `/` works: not browser tested
- Whether app root `/` is expected to work: not assessed in this QA pass
- Whether `/ui/steel-guitar-rag-mock.html` works: not browser tested
- Whether `/ui/steel-guitar-rag-mock.html` is expected to work: not assessed in this QA pass
- Who should test this URL: Codex automated QA only for this task
- Do not test these URLs: do not infer protected-preview browser behavior from local API fallback
- Known caveats: direct local POST to `127.0.0.1:8770/api/answer` requires Cloudflare Access identity, so focused and strict QA used the repo's local eval app harness on temporary loopback ports

## Pass/Fail Decision

Pass for the requested automated QA scope.

True P1 blockers found: 0.

The protected-preview browser/API target was not directly testable without Cloudflare Access identity, but the current local runtime version endpoint confirmed HEAD `05e8748`. The fallback harness exercised the current worktree/runtime code at the same HEAD.

## Focused QA Results

Focused automated API fallback smoke output: `/tmp/pocketsteel-05e8748-focused-qa.json`.

Total prompts: 36.

Automated raw result: 34 pass, 2 fail.

Corrected true-blocker result: 36 pass, 0 true blockers.

Category results:

- resolver-fix prompts: 8/8 true pass
- SGF leakage traps: 6/6 true pass
- off-domain bait: 4/4 true pass
- chord/fretboard prompts: 8/8 true pass
- repair prompts: 5/5 true pass
- Steel Guitar Rag/song prompts: 5/5 true pass

The two raw automated failures were scorer-pattern noise, not product blockers:

- `give me a real lesson now`: flagged `forum_first_person_fragment` because the answer contained `I-to-IV`; this is music theory notation, not a first-person forum fragment.
- `Where can I find a steel guitar repair person?`: flagged `forum_first_person_fragment` because the answer began `I do not have a current live directory`; this is a direct capability caveat, not raw SGF/forum text.

## Strict Scorer Results

Strict scorer outputs:

- `/tmp/pocketsteel-05e8748-strict-answer-quality.md`
- `/tmp/pocketsteel-05e8748-strict-answer-quality.json`

Total questions: 295.

Outcome counts:

- pass: 152
- warn: 33
- fail: 110

Strict SGF leakage gates:

- `sgf_primary_answer_leakage`: 0
- `forum_first_person_fragment_leakage`: 0
- `deterministic_teacher_answer_source_fragment`: 0
- `repair_instruction_source_fragment_failure`: 0
- `weak_source_boilerplate_in_answer`: 0
- `off_domain_sgf_source_cards`: 0
- `lesson_request_no_lesson`: 0
- `scale_request_missing_notes`: 0
- `lick_request_missing_playable_tab`: 0

Remaining strict failures are concentrated in older/broader eval-policy buckets, especially `missing_sources`, `legacy_source_weakness___no-source`, `legacy_likely_directness_failure`, wrong-key/enharmonic deterministic routing expectations, and vendor/source-required expectations. Those are not true blockers for the requested resolver/SGF leakage/off-domain/chord/repair/song QA pass.

One strict row, `Can you play Panhandle Rag with a pan handle?`, was marked by the scorer as `song_title_unrelated_theory_fragments`, but the answer was a direct joke/guardrail-style steel-bar explanation rather than unrelated theory fragments. This should be treated as scorer calibration noise unless product policy wants that prompt handled differently.

## True Blockers

None found in the requested automated QA scope.

Specifically:

- no raw SGF/forum/source fragments appeared as primary answer text in the focused QA set
- no weak-source warning appeared as primary answer text in the focused QA set
- off-domain bait returned guardrail behavior without source cards
- chord/fretboard prompts returned deterministic/teacher-first behavior with expected visual routing where applicable
- repair prompts returned synthesized practical guidance rather than raw forum fragments
- Steel Guitar Rag/song prompts did not fall through to raw SGF primary text

## Files Changed

Created:

- `docs/handoffs/task-completions/2026-06-14-1700-15-runtime-05e8748-automated-qa.md`

Generated artifacts outside the repo:

- `/tmp/pocketsteel-05e8748-focused-qa.json`
- `/tmp/pocketsteel-05e8748-strict-answer-quality.md`
- `/tmp/pocketsteel-05e8748-strict-answer-quality.json`

No implementation files were changed.

## Tests And Checks

Commands run:

```bash
git status --short && git rev-parse --short HEAD && git log --oneline -8
curl -sS -i http://127.0.0.1:8770/api/version | head -30 || true
curl -sS -i https://app.steelguitarrag.com/api/version | head -30 || true
curl -sS -i -X POST http://127.0.0.1:8770/api/answer -H 'Content-Type: application/json' --data '{"question":"What is the capital of France?","mode":"ask","topK":6}' | head -40
.venv/bin/python scripts/run_full_answer_quality_eval.py --output /tmp/pocketsteel-05e8748-strict-answer-quality.md --json-output /tmp/pocketsteel-05e8748-strict-answer-quality.json
.venv/bin/python -m pytest tests/test_answer_intent_classifier.py tests/test_api_search.py tests/test_full_answer_quality_eval.py tests/test_answer_eval.py -q
git diff --check
```

Results:

- local version endpoint: passed, reported HEAD `05e8748`
- protected-preview version endpoint: blocked by Cloudflare Access redirect
- direct local `/api/answer` POST on port `8770`: returned 401 because Cloudflare Access identity is required
- focused automated API fallback smoke: no true blockers
- strict scorer: no SGF-primary, forum-fragment, weak-source, or off-domain-source leakage hits
- focused pytest set: `376 passed in 1.96s`
- `git diff --check`: failed on pre-existing parked whitespace in `docs/answer-eval-report.md`; this file was not touched by this task

## Integration Notes

The runtime/version target is aligned with current HEAD `05e8748`, but protected-preview browser verification remains Cloudflare Access-dependent. This report should not be described as browser smoke.

The strict scorer still carries broad backlog failures. Those should not be conflated with the requested true-blocker QA result.

Recommended Lane 15 scorer calibration follow-up:

```text
Lane 15: calibrate broad strict scorer false positives from the 05e8748 QA run. Treat music-theory tokens such as I-to-IV as notation, not first-person forum fragments; treat direct capability caveats such as "I do not have a current live directory" as allowed answer text; review the song_title_unrelated_theory_fragments rule for joke/wordplay prompts such as "Can you play Panhandle Rag with a pan handle?" Do not modify backend behavior.
```

## Risk Assessment

Risk: low for QA reporting. The run was read-only except for this handoff and `/tmp` artifacts.

Rollback notes: remove this handoff if the report itself needs to be superseded.

## Commit Readiness

Needs human review first.

Reason: the QA result itself is pass, but the repo worktree contains many unrelated parked dirty/untracked files and `git diff --check` fails on pre-existing `docs/answer-eval-report.md` whitespace. No commit was requested.

## Suggested Next Step

Recommended lane: 15 QA / Answer Eval or 12 Self-Hosted Deployment, depending on whether the next objective is scorer calibration or authenticated browser verification.

Exact next prompt for QA calibration:

```text
Lane 15: calibrate the broad strict answer scorer after the 05e8748 automated QA pass. Fix only scorer/eval false positives for I-to-IV notation, direct capability caveats, and joke/song-title prompts; do not change backend answer behavior. Run focused evaluator tests and write a handoff.
```

Exact next prompt for authenticated protected-preview verification:

```text
Lane 12: run authenticated protected-preview browser smoke for HEAD 05e8748 using the Cloudflare Access session. Verify the same resolver, SGF leakage, off-domain, chord/fretboard, repair, and Steel Guitar Rag prompt set. Label results as protected-preview browser smoke and include the required Smoke Target block.
```
