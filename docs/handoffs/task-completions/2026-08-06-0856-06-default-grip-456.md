# Explorer default grip 4-5-6

## Outcome

The E9 Fretboard Explorer now opens with string grip `4-5-6` selected instead of displaying every core grip at once. The initial deterministic render is limited to the matching `4-5-6` positions and highlights, while manual selections and URL `grip` overrides continue to work.

## Files changed

- `ui/e9-fretboard-explorer.html`
- `ui/e9-fretboard-explorer-loader.js`
- `tests/test_e9_explorer_controls_ui.py`
- `tests/test_frontend_answer_ui.py`

The asset versions were changed to `default-grip-456-20260806` so the updated Explorer markup and script are not hidden behind an older cached loader.

## Verification

Passed:

- `git diff --check`
- `node --check ui/e9-fretboard-explorer.js`
- `node --check ui/e9-fretboard-explorer-loader.js`
- `node --check ui/e9-fretboard-explorer-config.js`
- `.venv/bin/python -m pytest -q tests/test_e9_explorer_controls_ui.py tests/test_frontend_answer_ui.py` — 53 passed
- `.venv/bin/python -m pytest -q tests/test_pedal_steel_fretboard_ui.py tests/test_explorer_musical_red_team.py tests/test_fretboard_explorer.py` — 87 passed
- `.venv/bin/python -m pytest -q tests/test_private_preview_launchagent.py::test_preflight_rejects_a_branch_checkout` after commit — 1 passed
- `.venv/bin/python -m pytest -q` after commit — 1,643 passed

## Local browser smoke

Smoke Target:

- Target type: local
- Result type: browser smoke
- Exact browser URL tested: `http://127.0.0.1:8898/ui/e9-fretboard-explorer.html?v=default-grip-456-local`
- Cache-busted URL tested: same as exact browser URL
- Auth required: no; static local Explorer payload smoke
- Auth provider: none
- Local backend URL: `http://127.0.0.1:8898`
- Expected backend port: `8898`
- Expected git HEAD: `39e24eff48a9` plus this scoped worktree change
- Version endpoint result: not required for local static UI smoke
- Root URL status: not tested; Explorer-specific route was the smoke target
- API fallback status: `/api/session` returned 404 as expected for the static server and the Explorer correctly fell back to its common deterministic profile
- Exact URL the user should test: protected production URL after deployment

Browser result:

- Initial string-group value was `4-5-6`.
- Initial render contained 9 result cards and 9 fretboard highlights, all for grip `4-5-6`.
- Direct URL override `mode=single&grip=6-8-10` selected `6-8-10` and rendered 5 cards plus 5 highlights, all for the requested override grip.
- Browser diagnostic log was empty.
- The temporary local browser tab and server were closed after smoke testing.

## Risk and rollback

Risk is low and limited to the Explorer's initial UI selection. No music rules, deterministic position data, API, schema, or retrieval behavior changed. Roll back the scoped feature commit to restore the former all-grips default.
