# String By Play-Along Key Fix

## Task Summary

- Fixed the Play Along analyzer failure that changed the supplied A-flat song
  into a long false E-flat region.
- Kept key-region scoring anchored to the chord candidate led by the audio,
  reduced the ability of a proposed key to rewrite a competing chord root, and
  raised the minimum stable key-region length from six to eight bars.
- Added deterministic regression coverage for dominant-heavy A-flat harmony
  with ambiguous B-flat-minor evidence.
- Advanced the local-analysis calibration and cache-busted worker/client/page
  assets so saved stale song maps are sent through Review Song for reanalysis
  instead of opening directly in Play Along.

## Lane Classification

- Lane 05 Backend / RAG Integration with Lane 15 QA / Answer Eval verification.
- MEDIUM-risk local analysis calibration and UI upgrade routing.

## Root Cause And Correction

The global detector already preferred A-flat, but `keyEvidenceScore` allowed a
proposed section key to select a weaker chord candidate after applying that
same key's harmonic prior. In the supplied recording, provisional E-flat
context could therefore reinterpret audio-leading B-flat minor as B-flat major
and then use that rewrite as further evidence for E-flat.

The correction makes key-region evidence use the audio-leading chord candidate,
weights the decoder key prior at `0.35`, and prevents context from changing to
a different root when the audio-leading candidate is at least `0.04` stronger.
An eight-bar minimum also rejects the remaining short false-region edge case
while preserving the existing thirteen-bar genuine-modulation fixture.

## Exact Recording Result

The supplied file was analyzed in place and was not copied, modified, or
committed:

- Starting key: A-flat major (`Ab major`)
- Key journey: A-flat major only, bars 1-153
- Key confidence: `0.587`
- B-flat-minor events retained: 32 final events, including all 25 events whose
  raw audio-leading candidate was `Bbm`
- Browser Review Song result: `Ab` selected, `Key journey: Ab major`, zero key
  region-change markers, and no browser console errors

## Tests And Checks

- Focused Play Along suites:
  `.venv/bin/python -m pytest -q tests/test_practice_analysis_v2.py tests/test_play_songs_ui.py tests/test_same_origin_smoke_server.py tests/test_practice_tools.py`
  -> `44 passed in 4.50s`
- Full Python suite: `.venv/bin/python -m pytest -q`
  -> `1692 passed in 88.43s`
- `npm run check:js` -> passed
- `npm run check:assets` -> passed for 1,372 tracked files and 51 Explorer
  chunks
- `npm run check:locks` -> passed for five environments
- `.venv/bin/python scripts/check_secret_patterns.py` -> passed
- `git diff --check` and staged diff check -> passed
- Exact MP3 worker reproduction after the fix -> A-flat major with one A-flat
  region and no false E-flat or F region
- Local same-origin browser upload/reanalysis -> passed with the exact MP3;
  no console errors
- Exact candidate based on the prior live release: focused suites, JavaScript,
  assets, dependency locks, secret patterns, detached-release preflight, and
  isolated LaunchAgent supervision -> passed
- Exact candidate full suite in its detached release: `1684 passed, 2 skipped`;
  two deployment tests failed only because they deliberately assert that
  preflight rejects their checkout, while this checkout is the exact detached
  release that preflight must accept. The full branch checkout suite remains
  `1692 passed`.

## Browser Smoke Target

- Target type: local same-origin smoke server
- Result: passed
- Exact entry URL:
  `http://127.0.0.1:8772/songs?v=audio-led-key-v11-local`
- Exact generated Review URL:
  `http://127.0.0.1:8772/setup/local-a9a17ddf-84bd-407f-a675-785fbd6375b9`
- Authentication: none; disposable local server
- Expected source state: uncommitted worktree based on `8356a25b`, later
  committed as `a32d8bf7`
- Version endpoint: not exposed by the disposable smoke fixture; exact loaded
  asset URLs confirmed the `audio-led-key-v11` client and
  `setup-song-audio-led-key-v12` page asset
- Root/canonical behavior: `/songs` uploaded the recording and navigated to the
  canonical `/setup/local-*` Review Song route
- API fallback: not applicable; this was a real browser upload and worker run

## Protected Preview Evaluation

- The broad development branch was not deployed. The scoped implementation
  commit was applied cleanly on top of the exact prior live release
  `29106242`, producing detached release `476a51e0`.
- Detached release path: `/Users/cory/.steel-rag/releases/476a51e0`
- Supervisor path:
  `/Users/cory/.steel-rag/supervisors/private-preview-476a51e0`
- The candidate first passed isolated user-LaunchAgent supervision and the
  exact MP3 browser flow on loopback port 8772.
- The password-free user LaunchAgent then handed port 8770 to `476a51e0`.
  No PID was killed, no system LaunchDaemon was changed, and no credential was
  printed or stored in the repository.
- Final live, ready, dependency, version, launchd-ownership, and listener checks
  passed. `/api/version` reports `476a51e`, and PID 87214 owns loopback port
  8770 under `com.steelguitarrag.private-preview`.
- Rollback plist preserved at
  `/Users/cory/.steel-rag/deploy-backups/com.steelguitarrag.private-preview.29106242-before-476a51e0.plist`.

## Protected Browser Smoke Target

- Target type: protected-preview
- Result: passed
- Exact cache-busted entry URL:
  `https://app.steelguitarrag.com/songs?v=audio-led-key-v11-476a51e-20260814`
- Exact user-smoke URL:
  `https://app.steelguitarrag.com/setup/local-4e5fb867-2306-412d-8011-b7ca43860794`
- Cloudflare Access: succeeded in the user's signed-in Chrome session
- Expected release: `476a51e044e8157d201c17aec9b659a944f76cb4`
- `/api/version`: `476a51e`
- Root behavior: not re-exercised; the scoped protected check used the canonical
  `/songs` -> `/setup/local-*` Play Along flow
- Canonical route: loaded successfully through Cloudflare Access
- API fallback: none; this was an authenticated browser check
- Loaded assets: `practice-analysis-client-audio-led-key-v11.js` and
  `setup-song-audio-led-key-v12.js` with their expected content digests
- Saved `String By` result: A-flat major, `Key journey: Ab major`, 32 `Bbm`
  bars in Chords mode, zero region-change markers, and zero browser console
  errors

## Files Changed

- `ui/practice-analysis-worker.js`
- `ui/practice-analysis-client.js`
- `ui/setup-song.js`
- `ui/setup-song.html`
- `ui/songs.js`
- `ui/songs.html`
- `ui/play-song.js`
- `ui/play-song.html`
- `ui/practice-analysis-worker-audio-led-key-v11.js`
- `ui/practice-analysis-client-audio-led-key-v11.js`
- `ui/setup-song-audio-led-key-v12.js`
- `ui/play-song-analysis-calibration-v7.js`
- `tests/test_practice_analysis_v2.py`
- `tests/test_play_songs_ui.py`
- `tests/test_same_origin_smoke_server.py`
- `docs/handoffs/task-completions/2026-08-14-1422-15-string-by-key-diagnosis.md`
- This handoff

## Commit

- Implementation commit: `a32d8bf7` (`fix: keep play along key analysis audio-led`)
- Initial handoff commit: `da300cb7` (`docs: record String By key fix`)
- Exact protected release: `476a51e0`, made by applying only `a32d8bf7` to the
  prior protected release `29106242`
- This deployment update is intended for a separate exact-path documentation
  commit.

## Risk Assessment

- Medium. The detector and context decoder are shared by all local song
  analysis, although genuine-modulation, borrowed-chord, seventh-quality,
  synthetic A-flat ambiguity, exact recording, focused UI, asset, and full
  Python coverage all pass.
- Previously analyzed projects intentionally re-run analysis when opened. Any
  manual chord edits are preserved by the existing upgrade path.

## Human Decision Needed

- No. The scoped correction is committed, deployed on an exact narrow release,
  and ready for user smoke.

## Safe-To-Stage Exact File List

- `docs/handoffs/task-completions/2026-08-14-1439-05-string-by-key-fix.md`

## Files That Must Not Be Staged

- `docs/handoffs/task-completions/integration-status.md` (pre-existing change)
- `docs/handoffs/task-completions/2026-08-04-1629-12-app-origin-502-diagnosis.md`
- `docs/handoffs/task-completions/2026-08-12-0714-18-rag-architecture-research-summary.md`
- `docs/handoffs/task-completions/2026-08-13-1323-15-huge-question-answer-regression.md`
- `output/`
- The supplied MP3 or any derived audio/PCM

## Recommended Next Lane

- User smoke on the exact protected Review Song URL above, followed by Repo
  Steward integration of `a32d8bf7` onto the eventual mainline release branch.
