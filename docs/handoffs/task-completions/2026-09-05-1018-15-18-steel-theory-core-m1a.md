# Shared Steel-Theory Core M1a Handoff

Date: 2026-09-05 10:18 CDT

Lane: `18 Product / Architecture` with focused `15 QA`

Primary scope: `PLATFORM:SHARED`

Secondary scope: none; product behavior was observed only through frozen M0
fixtures and existing tests

Model tier: `STANDARD`

Routing reason: implementation follows an approved, bounded shared contract;
the user supplied two additional musical acceptance rules during the slice

## Run Charter

- Objective: implement the pure shared E9 pitch/copedent foundation authorized
  as M1a and encode dominant-seventh plus A+B 5-6-7 ii-minor behavior as gold
  rules.
- Permitted paths: new `packages.steel_theory` sources and tests, package
  metadata, M1a contract/migration documentation, and this handoff.
- Forbidden paths: RAG and Companion runtime imports, APIs, browser code,
  persisted records, private copedents, product schemas, deployment, services,
  releases, DNS, and broad repository movement.
- Consumers to verify: the reviewed platform candidate fixture and the frozen
  RAG/Howdy contract characterization suites; neither product may consume the
  package in this slice.
- Budget: one pure-core slice, standard implementation model, no paid model/API
  calls, no runtime mutation, and no deployment.
- Stop conditions: any need to guess a private lever assignment, modify a
  product consumer, change persistence, or expand into the song/event model.
- Terminal state: `PASS`; M1a is implemented without runtime adoption.

## Task Summary

Completed:

- Added a product-neutral `packages.steel_theory` package for note names,
  scientific pitches, versioned copedents, pedal changes, resolved notes,
  chord formulas, exact grip analysis, position search, scale-degree identity,
  canonical snapshots, and snapshot digests.
- Added the reviewed `emmons-e9-basic` revision 1 profile with ten strings and
  only the approved A/B controls.
- Added fail-closed validation for unknown strings, controls, notes, chord
  qualities, out-of-range frets, repeated controls, repeated grip strings, and
  malformed copedent definitions.
- Encoded and tested three first grip families: no-pedal 4-5-6 major,
  no-pedal 4-5-6-9 dominant seventh, and A+B 5-6-7 ii minor.
- Proved the user's examples mechanically: F# minor at the nut in E and A minor
  at fret 3 in G for A+B 5-6-7; E7 at the nut and G7 at fret 3 for no-pedal
  4-5-6-9.
- Included the shared package in the built wheel and updated M1a status and
  next-gate wording in the platform documents.

Intentionally not changed:

- Neither Steel Guitar RAG nor Travis Companion imports this package.
- Existing answer routing, fretboard catalogs, product UI, APIs, saved data,
  private/user copedents, and deployment topology are unchanged.
- No product module was moved and no `apps/`, `services/`, or broader
  `packages/` reorganization was performed.
- Lever assignments and richer enharmonic spelling remain later, explicitly
  reviewed work.

## Files Changed

- `docs/platform-adapter-and-migration-plan.md`
- `docs/platform-migration-sequence.md`
- `docs/platform-shared-contract-v1.md`
- `docs/steel-theory-core.md`
- `packages/__init__.py`
- `packages/steel_theory/__init__.py`
- `packages/steel_theory/core.py`
- `pyproject.toml`
- `tests/test_platform_steel_theory.py`
- this handoff

No files were moved or deleted.

## Tests And Checks

- M1a plus M0 contract/characterization/boundary tests: 26 passed.
- Full locked Python 3.12 test environment: 1,666 passed. Three local
  host-layout checks failed because this isolated worktree intentionally has no
  `.venv` or installed private-preview release Python; the same checks passed
  in the prior hosted CI run, whose workflow creates the expected `.venv`.
- Ruff check: passed for the new package and tests.
- Ruff formatting check: passed after formatting the new package and tests.
- Mypy: passed for both shared source files.
- Platform dependency boundary check: passed; 72 managed source files scanned.
- Wheel build: passed and contains `packages/__init__.py`,
  `packages/steel_theory/__init__.py`, and `packages/steel_theory/core.py`.
- Candidate copedent snapshot and SHA-256 digest: exact fixture match.
- `git diff --check`: passed.

Hosted CI must still confirm the exact candidate commit. Existing dependency
audit failures are outside this slice and must not be attributed to M1a.

## Risk Assessment

Risk: low.

The new code is additive and unconsumed by either product. The principal future
risk is semantic drift when current product copedent shapes are projected into
the shared profile; M1b must compare them read-only before any consumer switch.

## Human Decision Needed

No decision is needed to merge M1a after exact-commit CI succeeds.

A separate approval is required before M1b projects an identified Howdy
copedent profile or before either product imports the shared package. Any
private/user-specific lever assignment must be supplied or explicitly approved,
not inferred.

## Safe-To-Stage Exact File List

- `docs/platform-adapter-and-migration-plan.md`
- `docs/platform-migration-sequence.md`
- `docs/platform-shared-contract-v1.md`
- `docs/steel-theory-core.md`
- `docs/handoffs/task-completions/2026-09-05-1018-15-18-steel-theory-core-m1a.md`
- `packages/__init__.py`
- `packages/steel_theory/__init__.py`
- `packages/steel_theory/core.py`
- `pyproject.toml`
- `tests/test_platform_steel_theory.py`

## Files That Must Not Be Staged

- `.venv/`, build directories, wheels, and package metadata generated during
  local verification
- every RAG or Companion runtime source file
- all deployment, release, corpus, vector, private-source, log, and Cloudflare
  state

## Recommended Next Lane

`18 Product / Architecture` plus `15 QA`: review and authorize M1b as read-only
copedent projections. Compare the characterized RAG profile and an explicitly
approved Howdy profile with M1a, including the dominant-seventh and ii-minor
gold cases. Do not change a product consumer in M1b.

## Commit Readiness

Safe for exact-path staging and commit. Before push, review the cached path list,
the complete cached diff, and `git diff --cached --check`. Hosted CI is the
exact-commit promotion evidence.
